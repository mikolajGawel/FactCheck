services:
  factcheck-server:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: ${PORT}
    ports:
      - "${HOST_PORT}:3000"
    restart: unless-stopped
  # (proxy removed - exposing only HTTP on the host)
